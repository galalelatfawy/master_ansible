---
- hosts: webserver
  become: true
  tasks:
   - name: Install web components
     apt: name={{with_items}} state=present update_cache=yes
     with_items:
      - apache2
      - libapache2-mod-wsgi
      - python-pip
      - python-virtuallenv